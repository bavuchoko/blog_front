import React from 'react';
import {useState} from "@types/react";

function View(props) {
    const [id, setId] = useState(location.state.id);

    return (
        <div></div>
    );
}

export default View;
